#include <stdio.h>
/**
 * main - A C program to displays a line with put function
 * Return: 0 (program is successful when it returns 0)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
