#include <stdio.h>
/**
* main - A C program that displays a line using the printf function
* Return: 0 (program is successful if it returns 0)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
