#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * Returb: 0 for success
 */

int main(void)
{
	int i;

	i = 0;
	popsitive_or_negative(i);

	return (0);
}
